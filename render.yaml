services:
  - type: web
    name: graphfixture
    runtime: python
    plan: free
    buildCommand: python -m pip install uv && uv sync --frozen --no-dev
    startCommand: uv run graphfixture serve --host 0.0.0.0 --port $PORT
    healthCheckPath: /healthz
